<Page
    x:Class="Common_Position.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Common_Position"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <Grid Background="SteelBlue" Width="400" Height="400">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBox Grid.Column="0" Grid.Row="0" FontSize="20"
                       Text="左上に配置" HorizontalAlignment="Left"
                       VerticalAlignment="Top" />
            <TextBox Grid.Column="0" Grid.Row="1" FontSize="20"
                       Text="右下に配置" HorizontalAlignment="Right"
                       VerticalAlignment="Bottom" />
            <TextBox Grid.Column="0" Grid.Row="2" FontSize="20"
                       Text="いっぱいに配置" HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch" Width="200" />
        </Grid>

    </Grid>
</Page>
