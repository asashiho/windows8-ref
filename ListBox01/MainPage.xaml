<Page
    x:Class="ListBox01.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ListBox01"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <StackPanel Orientation="Horizontal" >
            <!-- ListBoxItemを表示 -->
            <ListBox Margin="50">
                <ListBoxItem>
                    <TextBlock Text="フォントサイズ 15" FontSize="15" />
                </ListBoxItem>
                <ListBoxItem>
                    <TextBlock Text="フォントサイズ 20" FontSize="20" />
                </ListBoxItem>
            </ListBox>
            <!-- 操作できないリストボックスを表示 -->
            <ListBox Margin="50" IsEnabled="False" SelectedValue="項目2">
                <x:String>項目1</x:String>
                <x:String>項目2</x:String>
            </ListBox>
        </StackPanel>

    </Grid>
</Page>
